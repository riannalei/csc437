{
  "name": "proto",
  "version": "1.0.0",
  "private": true,
  "main": "index.html",
  "scripts": {
    "dev": "npx vite",
    "build": "npx tsc && npx vite build",
    "start": "http-server dist -p 3000",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop proto-server",
    "pm2:restart": "pm2 restart proto-server",
    "pm2:delete": "pm2 delete proto-server",
    "pm2:logs": "pm2 logs proto-server",
    "pm2:status": "pm2 status"
  },
  "dependencies": {
    "@calpoly/mustang": "^1.0.15",
    "http-server": "^14.1.1",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.4.1"
  }
}
