{
  "name": "proto",
  "version": "1.0.0",
  "private": true,
  "main": "index.html",
  "scripts": {
    "dev": "npx vite",
    "build": "npx tsc && npx vite build",
    "start": "http-server dist -p 3000",
    "pm2:start": "npx pm2 start ecosystem.config.js",
    "pm2:stop": "npx pm2 stop proto-server",
    "pm2:restart": "npx pm2 restart proto-server",
    "pm2:delete": "npx pm2 delete proto-server",
    "pm2:logs": "npx pm2 logs proto-server",
    "pm2:status": "npx pm2 status",
    "pm2:save": "npx pm2 save",
    "pm2:startup": "npx pm2 startup"
  },
  "dependencies": {
    "@calpoly/mustang": "^1.0.15",
    "http-server": "^14.1.1",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "pm2": "^5.3.0",
    "typescript": "^5.9.3",
    "vite": "^6.4.1"
  }
}
